import { DirectiveSample1Directive } from './directive-sample1.directive';

describe('DirectiveSample1Directive', () => {
  it('should create an instance', () => {
    const directive = new DirectiveSample1Directive();
    expect(directive).toBeTruthy();
  });
});
