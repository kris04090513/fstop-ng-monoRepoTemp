import { PipeSample1Pipe } from './pipe-sample1.pipe';

describe('PipeSample1Pipe', () => {
  it('create an instance', () => {
    const pipe = new PipeSample1Pipe();
    expect(pipe).toBeTruthy();
  });
});
